query domainsStats($ids: [Int]!, $from: String!, $to: String!) {
    domainsStats(ids: $ids, from: $from, to: $to) {
        id
        time
        DomainId
        vlynt
        fallback
        region
        category
    }
}

